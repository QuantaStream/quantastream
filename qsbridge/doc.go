// Package qsbridge contains the native SQL bridge planning vocabulary.
//
// The package is intentionally separate from qlbridge. The existing qlbridge
// fork remains the default compatibility path while qsbridge grows behind an
// opt-in native-engine path.
package qsbridge
